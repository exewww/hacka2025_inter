[project]
name = "backend"
version = "0.1.0"
description = ""
authors = [
    {name = "Your Name", email = "you@example.com"}
]
requires-python = ">=3.10,<4.0"

dependencies = [
    "flask",
    "flask-cors",
    "openai",
    "python-dotenv",
    "google-generativeai (>=0.8.5,<0.9.0)"
]

[tool.poetry]
package-mode = false   # ✅ Must be here, not under [project]

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
